// packages/domain — §5 domain model: TypeScript types and the Zod schemas for
// entities that cross the API boundary (Stage A3). No logic, no money maths
// (R1: every financial figure comes from packages/finance).

export * from './types';
export * from './schemas';
