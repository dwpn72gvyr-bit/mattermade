import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Portfolio() {
  return <EmptyState title="Portfolio is on its way" body="This screen lands with its build stage." />;
}
