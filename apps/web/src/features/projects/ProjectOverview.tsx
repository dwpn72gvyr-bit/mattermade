import React from 'react';
import { EmptyState } from '../../components/ui';

export default function ProjectOverview() {
  return <EmptyState title="ProjectOverview is on its way" body="This screen lands with its build stage." />;
}
