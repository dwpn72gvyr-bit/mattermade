import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Home() {
  return <EmptyState title="Home is on its way" body="This screen lands with its build stage." />;
}
