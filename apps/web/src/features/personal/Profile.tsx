import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Profile() {
  return <EmptyState title="Profile is on its way" body="This screen lands with its build stage." />;
}
