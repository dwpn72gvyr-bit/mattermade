import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Insights() {
  return <EmptyState title="Insights is on its way" body="This screen lands with its build stage." />;
}
