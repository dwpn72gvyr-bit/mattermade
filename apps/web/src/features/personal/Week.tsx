import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Week() {
  return <EmptyState title="Week is on its way" body="This screen lands with its build stage." />;
}
