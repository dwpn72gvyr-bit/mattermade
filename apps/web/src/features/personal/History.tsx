import React from 'react';
import { EmptyState } from '../../components/ui';

export default function History() {
  return <EmptyState title="History is on its way" body="This screen lands with its build stage." />;
}
