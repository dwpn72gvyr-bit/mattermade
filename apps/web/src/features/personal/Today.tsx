import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Today() {
  return <EmptyState title="Today is on its way" body="This screen lands with its build stage." />;
}
