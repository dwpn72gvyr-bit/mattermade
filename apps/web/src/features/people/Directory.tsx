import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Directory() {
  return <EmptyState title="Directory is on its way" body="This screen lands with its build stage." />;
}
