import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Pipeline() {
  return <EmptyState title="Leads & pipeline is on its way" body="This screen lands with the business development build." />;
}
