import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Overheads() {
  return <EmptyState title="Overheads is on its way" body="This screen lands with its build stage." />;
}
