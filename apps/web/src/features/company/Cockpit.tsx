import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Cockpit() {
  return <EmptyState title="Cockpit is on its way" body="This screen lands with its build stage." />;
}
