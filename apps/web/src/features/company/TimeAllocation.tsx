import React from 'react';
import { EmptyState } from '../../components/ui';

export default function TimeAllocation() {
  return <EmptyState title="TimeAllocation is on its way" body="This screen lands with its build stage." />;
}
