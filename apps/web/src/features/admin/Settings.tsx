import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Settings() {
  return <EmptyState title="Settings is on its way" body="This screen lands with its build stage." />;
}
