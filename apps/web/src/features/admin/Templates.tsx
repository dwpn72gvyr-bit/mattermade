import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Templates() {
  return <EmptyState title="Templates is on its way" body="This screen lands with its build stage." />;
}
