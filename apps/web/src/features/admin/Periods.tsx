import React from 'react';
import { EmptyState } from '../../components/ui';

export default function Periods() {
  return <EmptyState title="Periods is on its way" body="This screen lands with its build stage." />;
}
