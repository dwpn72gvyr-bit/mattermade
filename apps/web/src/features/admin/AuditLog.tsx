import React from 'react';
import { EmptyState } from '../../components/ui';

export default function AuditLog() {
  return <EmptyState title="AuditLog is on its way" body="This screen lands with its build stage." />;
}
