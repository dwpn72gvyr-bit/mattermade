import React from 'react';
import { EmptyState } from '../../components/ui';

export default function PlanQuoteList() {
  return <EmptyState title="PlanQuoteList is on its way" body="This screen lands with its build stage." />;
}
