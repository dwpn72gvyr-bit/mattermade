{
  "name": "oe-console",
  "version": "0.1.0",
  "private": true,
  "description": "OuterEdit Agency Intelligence Console",
  "scripts": {
    "dev": "pnpm --filter @oe/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r --no-bail test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": { "node": ">=20" }
}
